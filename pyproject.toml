[project]
name = "org-warrior"
version = "0.1.0"
description = "TaskWarrior-like CLI interface for org-ql"
requires-python = ">=3.9"

[tool.ruff]
line-length = 120
target-version = "py39"

[tool.pytest.ini_options]
testpaths = ["tests"]
